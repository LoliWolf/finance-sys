# Copy this file to bootstrap_go122.env. This is the project's current Nacos
# address; replace it only when developing from another network/environment.
NACOS_SERVER_ADDR=192.168.31.234:8848

# Code defaults: namespace=public, group=DEFAULT_GROUP, dataId=expert_trade.
# All business/runtime configuration, including the HTTP port, lives in Nacos.
